<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUI3Testing.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3Testing"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WinUI3Testing">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{StaticResource NavigationBackButtonNormalStyle}" ToolTipService.ToolTip="返回" VerticalAlignment="Top"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="ctlTitleBar">
                <TextBlock Padding="10,10,0,0" FontWeight="Bold">迈达克本地跟单系统</TextBlock>
            </StackPanel>

            <MenuBar Grid.Column="2" Margin="10,-3,0,0">
                <MenuBarItem Title="菜单一">
                    <MenuFlyoutItem Text="子菜单一"></MenuFlyoutItem>
                    <MenuFlyoutItem Text="子菜单二"></MenuFlyoutItem>
                    <MenuFlyoutItem Text="子菜单三"></MenuFlyoutItem>
                    <MenuFlyoutItem Text="子菜单四"></MenuFlyoutItem>
                </MenuBarItem>
                <MenuBarItem Title="菜单二">
                    <MenuFlyoutItem Text="子菜单一"></MenuFlyoutItem>
                </MenuBarItem>
                <MenuBarItem Title="菜单三">
                    <MenuFlyoutItem Text="子菜单一"></MenuFlyoutItem>
                    <MenuFlyoutItem Text="子菜单二"></MenuFlyoutItem>
                </MenuBarItem>
            </MenuBar>

            <Frame Grid.Column="3" x:Name="ctlTitleBlank"></Frame>

        </Grid>
        <Frame Grid.Row="1" x:Name="ctlMainPageFrame" x:FieldModifier="public" Padding="10"></Frame>
    </Grid>
</Window>
