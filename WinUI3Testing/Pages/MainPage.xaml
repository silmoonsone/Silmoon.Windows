<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUI3Testing.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3Testing.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:r="using:WinUI3Testing"
    d:DataContext="{d:DesignInstance Type=local:MainPageViewModel}"
    mc:Ignorable="d">

    <Grid>
        <Frame BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Padding="5">
            <StackPanel Spacing="10">
                <TextBox Text="{x:Bind ViewModel.Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="网址"></TextBox>
                <Button Content="Open Url in browser" Command="{StaticResource UrlOpenCommand}" CommandParameter="{x:Bind ViewModel.Url, Mode=TwoWay}" IsEnabled="{x:Bind ViewModel.Url, Converter={StaticResource NullOrDefaultToBoolConverter}}"></Button>
            </StackPanel>
        </Frame>
    </Grid>
</Page>
